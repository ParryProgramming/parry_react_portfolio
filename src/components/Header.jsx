// importing React library
import React from 'react';

// importing CSS for the Header component
import '../css/Header.css';

// header component
function Header() {
  // returning a header element with a class of "header" 
  return <header className="header">Alberto Lawant</header>;
}

// exporting the Header component to be used in other parts of the application
export default Header;